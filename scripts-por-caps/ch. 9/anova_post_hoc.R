# tras anova determinamos que existe al menos
# un algoritmo cuyo tiempo promedio de ejec.
# es != al de los demás

# queremos saber cuál es el mejor o el peor grupo comparado
# debemos saber si A y B difieren significativamente
# o B y C, o A y C.
# en general, con k grupos, la cantidad de comparaciones
# que deberíamos efectuar es:
# N = ( k 2 ) = k(k-1)/2

# Prueba HSD de Tukey > Bonferroni y Holm
